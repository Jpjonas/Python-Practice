# -*- encoding: utf-8 -*-

def activar_rayo():
    """
        La función permite al Dr. Terror activar su rayo
        destructor. Para esto toma un valor numérico por teclado
        y verifica que el mismo se un número de 4 cifras. De ser
        válido limpia la pantalla para que quede "oculto".
        Args:
            -
        Returns:
            Devuelve el código secreto ingresado.

    """
    # Ingrese aquí el código de la función. Tenga en cuenta
    # que debe devolver el valor del código secreto
    return codigo_secreto


def good_luck_max(coigo_secreto, numero_oportunidades):
    """
        La función toma un código y un número de oportunidades
        y compara el número intgresado con el código secreto,
        indicando en cada caso si el número ingresado es menor,
        mayor o igual al código secreto.
        Args:
            codigo_secrete: argumento de tipo entero que indica el
                codigo secteto que el dr terror eligió para su rayo
                mortal
            numero_oportunidades: numero de oportunidades con las que cuenta
                nuestro héroe para adivinar el código secreto
        Returns:
            returns: la función no posee valor de retorno, solo imprime por pantalla
    """
    # Ingrese aquí el código de la función



# La siguiente línea indica la acción que se realizará
# al llamar el archivo desde la terminal.
codigo_secreto = activar_rayo()
good_luck_max(codigo_secreto, 10)
